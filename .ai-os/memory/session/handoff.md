# Handoff

starter-first-session

Next action: Create the first common issue with `node scripts/pokit-issue-create.mjs --title "첫 작업"`.

Starter note: no prior chat memory, global memory, run history, or event receipts are required.
